import { create } from "@storybook/theming";

export default create({
  base: "dark",
  brandTitle: "Tailwind UIKIT",
  brandUrl: "https://github.com/gauravverma029/react-tailwind-uikit",
  /* docs: create({
    base: "dark",
    fontBase: "Calibre, Arial, Helvetica, sans-serif",
    textColor: "black",
  }), */
});
